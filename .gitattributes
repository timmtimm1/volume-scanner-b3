* text=auto eol=lf
*.png binary
*.ico binary
# Fixture posicional: viaja byte a byte, sem conversao de quebra de linha.
tests/fixtures/*.txt binary
# Recortes dos CSVs da CVM: latin-1 com CRLF, como a CVM publica. Converter a
# quebra de linha faria o teste rodar sobre um arquivo diferente do real.
tests/fixtures/cvm/*.csv binary
